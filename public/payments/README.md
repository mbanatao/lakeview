# Goodvibes LTD payment asset

`goodvibes-gcash-qr.svg` contains the supplied GCash QR image used for the manual website activation flow.

The QR must not be interpreted as an automatic payment webhook. Activation occurs only after Goodvibes LTD reviews the submitted receipt and approves the request in `/goodvibes-admin`.
