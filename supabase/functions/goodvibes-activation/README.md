# Goodvibes activation Edge Function

The active function is deployed to the RealMatch Supabase project as `goodvibes-activation`.

It provides server-controlled preview access, signed receipt uploads, payment submission, administrator listing, approval, rejection, and permanent property-site activation.

The function authenticates administrative actions by validating the Supabase access token and requiring the email `markjohnsonbanatao888@gmail.com`.
