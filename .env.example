# Property image CDN prefix. Leave blank to serve images from /public/property.
VITE_IMAGE_BASE=

# The Goodvibes activation client uses the RealMatch Supabase publishable key.
# Secrets and the service-role key are held only by the deployed Supabase Edge Function.
